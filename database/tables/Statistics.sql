CREATE TABLE Statistics (
    Stat_id     INTEGER PRIMARY KEY,
    Base_hp     INTEGER,
    Base_att    INTEGER,
    Base_def    INTEGER,
    Base_spatt  INTEGER,
    Base_spdef  INTEGER,
    Base_spd    INTEGER,
    Base_total  INTEGER
);